"use client";

import { useMemo, useState } from "react";
import { ArrowUpRight, CheckCircle2, Clock3, Copy, Filter, Mail, Megaphone, MessageSquare, MoreHorizontal, Play, Plus, Send, Tag, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { CampaignModal } from "@/components/campaigns/campaign-modal";
import { campaigns as initialCampaigns, type CampaignRecord, formatCurrency } from "@/lib/crm-data";

const statusFilters = ["Todas", "Activas", "Agendadas", "Rascunhos", "Concluídas"];
const statusStyles: Record<CampaignRecord["status"], string> = { Activa: "border-success/20 bg-success/5 text-success", Agendada: "border-accent/25 bg-accent/5 text-accent", Rascunho: "border-border-subtle bg-muted/60 text-muted-foreground", Concluída: "border-blue-500/20 bg-blue-500/5 text-blue-700" };

export function CampaignsContent() {
  const [campaigns, setCampaigns] = useState(initialCampaigns);
  const [activeStatus, setActiveStatus] = useState("Todas");
  const [modalOpen, setModalOpen] = useState(false);
  const [search, setSearch] = useState("");
  const filtered = useMemo(() => campaigns.filter((campaign) => { const matchesStatus = activeStatus === "Todas" || campaign.status === activeStatus.slice(0, -1) || (activeStatus === "Rascunhos" && campaign.status === "Rascunho"); const matchesSearch = `${campaign.name} ${campaign.audience} ${campaign.channel}`.toLowerCase().includes(search.trim().toLowerCase()); return matchesStatus && matchesSearch; }), [activeStatus, campaigns, search]);
  const activeCount = campaigns.filter((campaign) => campaign.status === "Activa").length;
  const revenue = campaigns.reduce((total, campaign) => total + campaign.revenue, 0);

  const addCampaign = (campaign: CampaignRecord) => { setCampaigns((current) => [campaign, ...current]); setModalOpen(false); };

  return (
    <div className="space-y-7 pb-12">
      <div className="flex flex-col gap-5 border-b border-border-subtle pb-6 lg:flex-row lg:items-end lg:justify-between"><div><div className="mb-3 inline-flex items-center gap-2 text-[11px] font-semibold text-muted-foreground"><Megaphone className="h-3.5 w-3.5 text-accent" /> Activação e relacionamento</div><h2 className="text-[1.85rem] font-extrabold tracking-[-0.04em] text-foreground sm:text-3xl">Campanhas</h2><p className="mt-2 max-w-2xl text-xs leading-relaxed text-muted-foreground sm:text-sm">Planeie comunicações segmentadas, acompanhe resultados e mantenha cada activação ligada ao cliente.</p></div><Button variant="accent" size="sm" onClick={() => setModalOpen(true)} className="h-9 self-start text-xs font-semibold sm:self-auto"><Plus className="h-3.5 w-3.5" /> Nova campanha</Button></div>

      <section className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-4"><article className="card-surface border-l-2 border-l-accent p-4 sm:p-5"><p className="page-kicker">Receita influenciada</p><p className="mt-4 text-2xl font-extrabold tracking-[-0.04em]">{formatCurrency(revenue)}</p><p className="mt-2 text-[11px] font-bold text-success">+18,6% <span className="font-medium text-muted-foreground">vs período anterior</span></p></article><article className="card-surface p-4 sm:p-5"><p className="page-kicker">Campanhas activas</p><p className="mt-4 text-2xl font-extrabold tracking-[-0.04em]">{activeCount}</p><p className="mt-2 text-[11px] font-medium text-muted-foreground">em acompanhamento</p></article><article className="card-surface p-4 sm:p-5"><p className="page-kicker">Público alcançado</p><p className="mt-4 text-2xl font-extrabold tracking-[-0.04em]">1.204</p><p className="mt-2 text-[11px] font-bold text-success">+9,4% <span className="font-medium text-muted-foreground">clientes impactados</span></p></article><article className="card-surface p-4 sm:p-5"><p className="page-kicker">Taxa média de abertura</p><p className="mt-4 text-2xl font-extrabold tracking-[-0.04em]">72,1%</p><p className="mt-2 text-[11px] font-medium text-muted-foreground">nos canais monitorizados</p></article></section>

      <div className="data-surface overflow-hidden"><div className="flex flex-col gap-4 border-b border-border-subtle p-4 sm:p-5 lg:flex-row lg:items-center lg:justify-between"><div className="flex min-w-0 flex-wrap items-center gap-1 rounded-xl border border-border bg-background p-1">{statusFilters.map((status) => <button key={status} type="button" onClick={() => setActiveStatus(status)} className={`rounded-lg px-3 py-2 text-[11px] font-bold transition-colors ${activeStatus === status ? "bg-foreground text-primary-foreground" : "text-muted-foreground hover:bg-muted hover:text-foreground"}`}>{status}</button>)}</div><div className="relative w-full lg:w-72"><Filter className="pointer-events-none absolute left-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-muted-foreground" /><input value={search} onChange={(event) => setSearch(event.target.value)} className="auth-input h-9 pl-9 text-xs" placeholder="Filtrar campanhas..." /></div></div><div className="divide-y divide-border-subtle">{filtered.map((campaign) => <article key={campaign.id} className="flex flex-col gap-4 p-4 transition-colors hover:bg-muted/30 sm:p-5 lg:flex-row lg:items-center"><div className="flex min-w-0 flex-1 items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-accent/20 bg-accent/10 text-accent">{campaign.channel === "E-mail" ? <Mail className="h-4 w-4" /> : campaign.channel === "WhatsApp" ? <MessageSquare className="h-4 w-4" /> : <Tag className="h-4 w-4" />}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="truncate text-sm font-bold text-foreground">{campaign.name}</h3><span className={`rounded-lg border px-2 py-1 text-[10px] font-bold ${statusStyles[campaign.status]}`}>{campaign.status}</span></div><p className="mt-1 text-[11px] text-muted-foreground">{campaign.audience} · {campaign.updatedAt}</p><div className="mt-2 flex flex-wrap items-center gap-3 text-[10px] font-semibold text-muted-foreground"><span className="inline-flex items-center gap-1"><Send className="h-3 w-3" /> {campaign.channel}</span><span className="inline-flex items-center gap-1"><Users className="h-3 w-3" /> {campaign.sent || "—"} enviados</span></div></div></div><div className="grid grid-cols-3 gap-4 border-t border-border-subtle pt-3 sm:flex sm:items-center sm:border-t-0 sm:pt-0"><div><p className="page-kicker">Abertura</p><p className="mt-1 text-xs font-bold text-foreground">{campaign.openRate}</p></div><div><p className="page-kicker">Cliques</p><p className="mt-1 text-xs font-bold text-foreground">{campaign.clickRate}</p></div><div><p className="page-kicker">Receita</p><p className="mt-1 text-xs font-bold text-foreground">{campaign.revenue ? formatCurrency(campaign.revenue) : "—"}</p></div><button type="button" aria-label={`Opções da campanha ${campaign.name}`} className="hidden rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground sm:block"><MoreHorizontal className="h-4 w-4" /></button></div></article>)}{filtered.length === 0 ? <div className="p-10 text-center"><p className="text-sm font-bold text-foreground">Nenhuma campanha encontrada</p><p className="mt-1 text-xs text-muted-foreground">Ajuste o filtro ou crie uma nova campanha.</p></div> : null}</div></div>

      <div className="grid gap-4 xl:grid-cols-3"><article className="data-surface p-4 sm:p-5 xl:col-span-2"><div className="flex items-center justify-between"><div><p className="page-kicker">Próxima activação</p><h3 className="mt-1 text-base font-bold tracking-tight">Recompra de agosto</h3></div><span className="inline-flex items-center gap-1.5 rounded-lg border border-success/20 bg-success/5 px-2 py-1 text-[10px] font-bold text-success"><CheckCircle2 className="h-3 w-3" /> Activa</span></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-border-subtle bg-background/70 p-3"><p className="page-kicker">Público</p><p className="mt-2 text-lg font-extrabold">386</p><p className="mt-1 text-[10px] text-muted-foreground">clientes prontos</p></div><div className="rounded-xl border border-border-subtle bg-background/70 p-3"><p className="page-kicker">Conversão</p><p className="mt-2 text-lg font-extrabold">22,8%</p><p className="mt-1 text-[10px] text-muted-foreground">cliques no WhatsApp</p></div><div className="rounded-xl border border-border-subtle bg-background/70 p-3"><p className="page-kicker">Receita</p><p className="mt-2 text-lg font-extrabold">R$ 18.420</p><p className="mt-1 text-[10px] text-muted-foreground">atribuída à campanha</p></div></div></article><aside className="data-surface p-4 sm:p-5"><div className="flex items-start gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-accent/10 text-accent"><Clock3 className="h-4 w-4" /></div><div><p className="text-xs font-bold text-foreground">Operação recomendada</p><p className="mt-1 text-[11px] leading-relaxed text-muted-foreground">Revise o rascunho de boas-vindas antes de activar o canal de E-mail.</p></div></div><Button variant="outline" size="sm" onClick={() => setModalOpen(true)} className="mt-4 h-9 w-full text-xs font-semibold"><Copy className="h-3.5 w-3.5" /> Criar a partir deste modelo</Button></aside></div>

      <CampaignModal open={modalOpen} onClose={() => setModalOpen(false)} audience={{ count: 386, label: "clientes seleccionados" }} onSaved={addCampaign} />
    </div>
  );
}
